{
  "name": "disease-prediction-cloudflare-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Disease Prediction API - Production Ready",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "test": "echo 'No tests configured'"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "api",
    "disease-prediction",
    "ml",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.46.0"
  }
}

