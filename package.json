{
  "name": "disease-prediction-system",
  "version": "1.0.0",
  "description": "Disease Prediction System - Cloudflare Deployment",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy public/",
    "publish": "npm run deploy",
    "build": "echo 'No build step required'"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "pages",
    "disease-prediction",
    "ml"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.46.0"
  }
}

